// build.gradle
def javaProjects = [project(':core'), project(':member')]
def bootProjects = [project(':member')]

allprojects {
    println('all projects tasks')
}

configure(javaProjects) {
    apply plugin: 'java'
    println('java projects tasks')
}

configure(bootProjects) {
    println('boot projects tasks')
}